services:
  app:
    # build:
    #   context: docker
    #   dockerfile: Dockerfile
    image: ghcr.io/densuke/sphinx-texlive:1743748074
    user: worker
    workdir: /app
    volumes:
      - .:/app:cached
    ports:
      - "8000:8000"
      